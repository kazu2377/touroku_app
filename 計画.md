## 実装計画（最小構成）

[未完] 1. Supabase 環境準備  
   - `.env.local` に `NEXT_PUBLIC_SUPABASE_URL` と `NEXT_PUBLIC_SUPABASE_ANON_KEY` を設定（テーブルと RLS は済み）。
[未完] 2. ライブラリ確認  
   - `@supabase/ssr` が導入済みか確認し、無ければ追加。
[未完] 3. Supabase ユーティリティ配置  
   - `lib/supabaseClient.ts` を指定コードで作成（browser/server クライアント）。
[未完] 4. レイアウト作成  
   - `app/layout.tsx` をシンプルな HTML ラッパーで用意。
[未完] 5. 認証ページ実装  
   - `app/page.tsx` に Google OAuth ログインボタン（`createClient` 使用）。
[未完] 6. ダッシュボード実装  
   - `app/dashboard/page.tsx` に目標投稿・一覧（auth.users から email 取得、最新順）を実装。
[未完] 7. ルーティング制御  
   - `middleware.ts` を追加し、未ログインの `/dashboard` へのアクセスを `/` にリダイレクト。
[未完] 8. 動作確認  
   - `/` → ログイン → `/dashboard` で投稿と一覧が動くか手動確認（必要に応じて Realtime や UI 改善は後続）。
